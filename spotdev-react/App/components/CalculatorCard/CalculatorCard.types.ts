export type CalculatorCardProps = {
    cardTitle: string;
    btnCalculateText: string;
}

export type CaldulatorCardHistoryProps = {
    id: number;
    firstNumber: number;
    secondNumber: number;
    operation: string;
    result: number;
}